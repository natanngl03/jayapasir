---
export interface CardProps {
   title: string;
   imgAlt: string;
}

interface Props extends CardProps {
   type: 'pasir' | 'batu';
}

const { title, imgAlt, type } = Astro.props;

const imgSrc = type == 'pasir' ? `/img/pasir/${imgAlt}.webp` : `/img/batu/${imgAlt}.webp`;
---

<div class='card shadow'>
   <a href={imgSrc} class='glightbox' data-gallery='gallery'>
      <img src={imgSrc} class='card-img-top cardimg' alt={imgAlt} width='100%' />
   </a>
   <div class='card-body'>
      <h6 class='card-title text-center mb-3'>{title}</h6>
      <a href={`/product/${imgAlt}`} class='btn btn-primary text-white d-flex align-items-center justify-content-center gap-2'>
         <i class='fas fa-long-arrow-alt-right'></i>
         <span>Cek Harga</span>
      </a>
   </div>
</div>

<style>
   .cardimg {
      width: 100%;
      height: 180px;
   }
   .card {
      border-radius: 10px;
      overflow: hidden;
   }
</style>
